HL.NotificationOptions
======================

.. class:: HL.NotificationOptions

   Table describing options for related API calls.

Shape
-----

.. code-block:: text

   {
       color? = string,
       timeout? = number,
       icon? = integer | string,
       font_size? = number,
   }

Fields
------

color : string, optional
   Color.

timeout : number, optional
   Timeout.

icon : integer | string, optional
   Icon.

font_size : number, optional
   Font size.

.. TODO: Replace generic field summaries with source-checked behavior.
