hl.exec_cmd
===========

.. function:: hl.exec_cmd(cmd: string, rules?: table<string, string|number|boolean>)

   Exec cmd.

Signature
---------

.. code-block:: text

   hl.exec_cmd(cmd: string, rules?: table<string, string|number|boolean>): nil

Parameters
----------

cmd : string
   Cmd.

rules : table<string, optional
   Rules.

string|number|boolean> : any
   Parameter.

Returns
-------

nil
   This function does not return a value.

Examples
--------

.. TODO: Add a minimal example.

See also
--------

:class:`HL.API`
   Namespace or API object containing this function.
